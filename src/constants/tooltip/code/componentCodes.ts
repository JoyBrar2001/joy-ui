export const bottomTooltipCode = `import { Button } from "@/components/ui/Button";
import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/Tooltip";

export function BottomTooltip() {
  return (
    <Tooltip>
      <TooltipTrigger>
        <Button variant="outline">
          Bottom
        </Button>
      </TooltipTrigger>

      <TooltipContent side="bottom">
        Hi, I'm the tooltip
      </TooltipContent>
    </Tooltip>
  );
}`;

export const destructiveTooltipCode = `import { Button } from "@/components/ui/Button";
import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/Tooltip";

export function DestructiveTooltip() {
  return (
    <Tooltip>
      <TooltipTrigger>
        <Button variant="destructive">
          Destructive
        </Button>
      </TooltipTrigger>

      <TooltipContent variant="destructive" side="top">
        Hi, I'm the tooltip
      </TooltipContent>
    </Tooltip>
  );
}`;

export const ghostTooltipCode = `import { Button } from "@/components/ui/Button";
import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/Tooltip";

export function GhostTooltip() {
  return (
    <Tooltip>
      <TooltipTrigger>
        <Button variant="ghost">
          Ghost
        </Button>
      </TooltipTrigger>

      <TooltipContent variant="ghost" side="bottom">
        Hi, I'm the tooltip
      </TooltipContent>
    </Tooltip>
  );
}`;

export const leftTooltipCode = `import { Button } from "@/components/ui/Button";
import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/Tooltip";

export function LeftTooltip() {
  return (
    <Tooltip>
      <TooltipTrigger>
        <Button variant="outline">
          Left
        </Button>
      </TooltipTrigger>

      <TooltipContent side="left">
        Hi, I'm the tooltip
      </TooltipContent>
    </Tooltip>
  );
}`;

export const outlineTooltipCode = `import { Button } from "@/components/ui/Button";
import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/Tooltip";

export function OutlineTooltip() {
  return (
    <Tooltip>
      <TooltipTrigger>
        <Button variant="outline">
          Outline
        </Button>
      </TooltipTrigger>

      <TooltipContent variant="outline" side="top">
        Hi, I'm the tooltip
      </TooltipContent>
    </Tooltip>
  );
}`;

export const primaryTooltipCode = `import { Button } from "@/components/ui/Button";
import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/Tooltip";

export function PrimaryTooltip() {
  return (
    <Tooltip>
      <TooltipTrigger>
        <Button variant="primary">
          Primary
        </Button>
      </TooltipTrigger>

      <TooltipContent variant="primary" side="top">
        Hi, I'm the tooltip
      </TooltipContent>
    </Tooltip>
  );
}`;

export const rightTooltipCode = `import { Button } from "@/components/ui/Button";
import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/Tooltip";

export function RightTooltip() {
  return (
    <Tooltip>
      <TooltipTrigger>
        <Button variant="outline">
          Right
        </Button>
      </TooltipTrigger>

      <TooltipContent side="right">
        Hi, I'm the tooltip
      </TooltipContent>
    </Tooltip>
  );
}`;

export const secondaryTooltipCode = `import { Button } from "@/components/ui/Button";
import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/Tooltip";

export function SecondaryTooltip() {
  return (
    <Tooltip>
      <TooltipTrigger>
        <Button variant="secondary">
          Secondary
        </Button>
      </TooltipTrigger>

      <TooltipContent variant="secondary" side="top">
        Hi, I'm the tooltip
      </TooltipContent>
    </Tooltip>
  );
}`;

export const simpleTooltipCode = `import { Button } from "@/components/ui/Button";
import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/Tooltip";

export function SimpleTooltip() {
  return (
    <Tooltip>
      <TooltipTrigger>
        <Button>
          Hover on me
        </Button>
      </TooltipTrigger>

      <TooltipContent>
        Hi, I'm the tooltip
      </TooltipContent>
    </Tooltip>
  );
}`;

export const topTooltipCode = `import { Button } from "@/components/ui/Button";
import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/Tooltip";

export function TopTooltip() {
  return (
    <Tooltip>
      <TooltipTrigger>
        <Button variant="outline">
          Top
        </Button>
      </TooltipTrigger>

      <TooltipContent side="top">
        Hi, I'm the tooltip
      </TooltipContent>
    </Tooltip >
  );
}`;