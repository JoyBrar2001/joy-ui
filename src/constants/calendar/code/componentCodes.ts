export const basicCalendarCode = `import { Calendar } from "@/components/ui/Calendar";\n\nexport function BasicCalendar() {\n  return <Calendar mode="single" />;\n}`;
export const dayPickerCode = `"use client";\n\nimport { useState } from "react";\nimport { Calendar } from "@/components/ui/Calendar";\n\nexport function DayPicker() {\n  const [date, setDate] = useState<Date>();\n  return <Calendar mode="single" selected={date} onSelect={setDate} />;\n}`;
export const rangePickerCode = `"use client";\n\nimport { useState } from "react";\nimport { Calendar } from "@/components/ui/Calendar";\nimport type { DateRange } from "react-day-picker";\n\nexport function RangePicker() {\n  const [range, setRange] = useState<DateRange>();\n  return <Calendar mode="range" selected={range} onSelect={setRange} />;\n}`;
export const twoMonthRangeCode = `"use client";\n\nimport { useState } from "react";\nimport { Calendar } from "@/components/ui/Calendar";\nimport type { DateRange } from "react-day-picker";\n\nexport function RangePickerTwoMonths() {\n  const [range, setRange] = useState<DateRange>();\n  return <Calendar mode="range" numberOfMonths={2} selected={range} onSelect={setRange} />;\n}`;
export const threeMonthCode = `import { Calendar } from "@/components/ui/Calendar";\n\nexport function ThreeMonthView() {\n  return <Calendar mode="single" numberOfMonths={3} />;\n}`;
export const multipleDayCode = `"use client";\n\nimport { useState } from "react";\nimport { Calendar } from "@/components/ui/Calendar";\n\nexport function MultipleDayPicker() {\n  const [dates, setDates] = useState<Date[]>([]);\n  return <Calendar mode="multiple" selected={dates} onSelect={setDates} />;\n}`;
export const footerCalendarCode = `"use client";\n\nimport { useState } from "react";\nimport { Calendar } from "@/components/ui/Calendar";\n\nexport function DatePickerWithFooter() {\n  const [date, setDate] = useState<Date>();\n  return <Calendar mode="single" selected={date} onSelect={setDate} footer="Please pick a day." />;\n}`;
export const disabledWeekendsCode = `import { Calendar } from "@/components/ui/Calendar";\n\nexport function CalendarWithDisabledWeekends() {\n  return <Calendar mode="single" disabled={{ dayOfWeek: [0, 6] }} />;\n}`;
