export const aspectRatioCode = `import { ComponentPropsWithoutRef, forwardRef } from "react";\nimport { cn } from "@/utils";\n\nexport const AspectRatio = forwardRef<HTMLDivElement, ComponentPropsWithoutRef<"div"> & { ratio?: number }>(\n  ({ ratio = 1, className, style, ...props }, ref) => (\n    <div ref={ref} className={cn("relative w-full overflow-hidden", className)} style={{ aspectRatio: ratio, ...style }} {...props} />\n  )\n);`;
