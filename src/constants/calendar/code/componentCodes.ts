export const basicCalendarCode = `import { Calendar } from "@/components/ui/Calendar";\n\nexport function BasicCalendar() {\n  return <Calendar mode="single" />;\n}`;
export const dayPickerCode = `"use client";\n\nimport { useState } from "react";\nimport { Calendar } from "@/components/ui/Calendar";\n\nexport function DayPicker() {\n  const [date, setDate] = useState<Date>();\n  return <Calendar mode="single" selected={date} onSelect={setDate} />;\n}`;
export const rangePickerCode = `"use client";\n\nimport { useState } from "react";\nimport { Calendar } from "@/components/ui/Calendar";\nimport type { DateRange } from "react-day-picker";\n\nexport function RangePicker() {\n  const [range, setRange] = useState<DateRange>();\n  return <Calendar mode="range" selected={range} onSelect={setRange} />;\n}`;
export const twoMonthRangeCode = `"use client";\n\nimport { useState } from "react";\nimport { Calendar } from "@/components/ui/Calendar";\nimport type { DateRange } from "react-day-picker";\n\nexport function RangePickerTwoMonths() {\n  const [range, setRange] = useState<DateRange>();\n  return <Calendar mode="range" numberOfMonths={2} selected={range} onSelect={setRange} />;\n}`;
export const threeMonthCode = `import { Calendar } from "@/components/ui/Calendar";\n\nexport function ThreeMonthView() {\n  return <Calendar mode="single" numberOfMonths={3} />;\n}`;
export const multipleDayCode = `"use client";\n\nimport { useState } from "react";\nimport { Calendar } from "@/components/ui/Calendar";\n\nexport function MultipleDayPicker() {\n  const [dates, setDates] = useState<Date[]>([]);\n  return <Calendar mode="multiple" selected={dates} onSelect={setDates} />;\n}`;
export const footerCalendarCode = `"use client";\n\nimport { useState } from "react";\nimport { Calendar } from "@/components/ui/Calendar";\n\nexport function DatePickerWithFooter() {\n  const [date, setDate] = useState<Date>();\n  return <Calendar mode="single" selected={date} onSelect={setDate} footer="Please pick a day." />;\n}`;
export const disabledWeekendsCode = `import { Calendar } from "@/components/ui/Calendar";\n\nexport function CalendarWithDisabledWeekends() {\n  return <Calendar mode="single" disabled={{ dayOfWeek: [0, 6] }} />;\n}`;

export const simpleCalendarCode = `"use client";\n\nimport { useState } from "react";\nimport { Calendar } from "@/components/ui/Calendar";\n\nexport function SimpleCalendar() {\n  const [date, setDate] = useState<Date>();\n  return <Calendar mode="single" selected={date} onSelect={setDate} />;\n}`;
export const calendarWithoutOutsideDaysCode = `import { Calendar } from "@/components/ui/Calendar";\n\nexport function CalendarWithoutOutsideDays() {\n  return <Calendar mode="single" showOutsideDays={false} />;\n}`;
export const calendarWithDisabledDatesCode = `import { Calendar } from "@/components/ui/Calendar";\n\nexport function CalendarWithDisabledDates() {\n  const disabled = [{ from: new Date(2026, 0, 10), to: new Date(2026, 0, 15) }];\n  return <Calendar mode="single" disabled={disabled} />;\n}`;
export const customStyledCalendarCode = `import { Calendar } from "@/components/ui/Calendar";\n\nexport function CustomStyledCalendar() {\n  return <Calendar mode="single" className="rounded-xl border-2 border-neutral-700 p-4" />;\n}`;
export const simpleRangeSelectorCode = `"use client";\n\nimport { useState } from "react";\nimport type { DateRange } from "react-day-picker";\nimport { Calendar } from "@/components/ui/Calendar";\n\nexport function SimpleRangeSelector() {\n  const [range, setRange] = useState<DateRange>();\n  return <Calendar mode="range" selected={range} onSelect={setRange} />;\n}`;
export const multipleDaysSelectorCode = `"use client";\n\nimport { useState } from "react";\nimport { Calendar } from "@/components/ui/Calendar";\n\nexport function MultipleDaysSelector() {\n  const [dates, setDates] = useState<Date[]>([]);\n  return <Calendar mode="multiple" selected={dates} onSelect={(value) => setDates(value ?? [])} />;\n}`;
export const calendarWithDropdownCode = `import { Calendar } from "@/components/ui/Calendar";\n\nexport function CalendarWithDropdown() {\n  return <Calendar mode="single" captionLayout="dropdown" fromYear={2020} toYear={2030} />;\n}`;
export const multipleMonthsSelectorCode = `"use client";\n\nimport { useState } from "react";\nimport type { DateRange } from "react-day-picker";\nimport { Calendar } from "@/components/ui/Calendar";\n\nexport function MultipleMonthsSelector() {\n  const [range, setRange] = useState<DateRange>();\n  return <Calendar mode="range" numberOfMonths={2} selected={range} onSelect={setRange} />;\n}`;
export const calendarWithPresetDaysCode = `"use client";\n\nimport { useState } from "react";\nimport { Calendar } from "@/components/ui/Calendar";\n\nexport function CalendarWithPresetDays() {\n  const [date, setDate] = useState<Date>(new Date());\n  return <Calendar mode="single" selected={date} onSelect={(value) => value && setDate(value)} />;\n}`;
export const calendarWithPresetRangeCode = `"use client";\n\nimport { useState } from "react";\nimport type { DateRange } from "react-day-picker";\nimport { Calendar } from "@/components/ui/Calendar";\n\nexport function CalendarWithPresetRange() {\n  const [range, setRange] = useState<DateRange>({ from: new Date(), to: new Date(Date.now() + 86400000 * 6) });\n  return <Calendar mode="range" selected={range} onSelect={setRange} />;\n}`;
export const dropdownCalendarCode = `"use client";\n\nimport { useState } from "react";\nimport { Calendar } from "@/components/ui/Calendar";\nimport { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/Popover";\nimport { Button } from "@/components/ui/Button";\n\nexport function DropddownCalendar() {\n  const [date, setDate] = useState<Date>();\n  return <Popover><PopoverTrigger asChild><Button variant="outline">Pick a date</Button></PopoverTrigger><PopoverContent><Calendar mode="single" selected={date} onSelect={setDate} /></PopoverContent></Popover>;\n}`;
export const dropdownCalendarWithPlaceholderCode = dropdownCalendarCode.replace("Pick a date", "Select your birthday");
export const dropdownCalendarWithDefaultValueCode = `"use client";\n\nimport { useState } from "react";\nimport { Calendar } from "@/components/ui/Calendar";\n\nexport function DropdownCalendarWithDefaultValue() {\n  const [date, setDate] = useState<Date | undefined>(new Date());\n  return <Calendar mode="single" selected={date} onSelect={setDate} />;\n}`;
export const dropdownRangeSelectorCode = `"use client";\n\nimport { useState } from "react";\nimport type { DateRange } from "react-day-picker";\nimport { Calendar } from "@/components/ui/Calendar";\n\nexport function DropdownRangeSelector() {\n  const [range, setRange] = useState<DateRange>();\n  return <Calendar mode="range" selected={range} onSelect={setRange} />;\n}`;
export const dropdownRangeWithMultipleMonthsCode = `"use client";\n\nimport { useState } from "react";\nimport type { DateRange } from "react-day-picker";\nimport { Calendar } from "@/components/ui/Calendar";\n\nexport function DropdownRangeWithMultipleMonths() {\n  const [range, setRange] = useState<DateRange>();\n  return <Calendar mode="range" numberOfMonths={3} selected={range} onSelect={setRange} />;\n}`;
export const dropdownRangeWithDefaultValueCode = `"use client";\n\nimport { useState } from "react";\nimport type { DateRange } from "react-day-picker";\nimport { Calendar } from "@/components/ui/Calendar";\n\nexport function DropdownRangeWithDefaultValue() {\n  const [range, setRange] = useState<DateRange>({ from: new Date(), to: new Date(Date.now() + 86400000 * 4) });\n  return <Calendar mode="range" selected={range} onSelect={setRange} />;\n}`;
