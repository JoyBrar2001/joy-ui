export const toggleCode = `import { ButtonHTMLAttributes, forwardRef, useState } from "react";\nimport { cn } from "@/utils";\n\nexport const Toggle = forwardRef<HTMLButtonElement, ButtonHTMLAttributes<HTMLButtonElement> & { pressed?: boolean; defaultPressed?: boolean; onPressedChange?: (pressed: boolean) => void }>(\n  ({ pressed, defaultPressed = false, onPressedChange, ...props }, ref) => {\n    const [uncontrolledPressed, setUncontrolledPressed] = useState(defaultPressed);\n    const isPressed = pressed ?? uncontrolledPressed;\n    return <button ref={ref} type="button" aria-pressed={isPressed} onClick={() => { const next = !isPressed; setUncontrolledPressed(next); onPressedChange?.(next); }} className={cn("rounded-md px-3 py-2", isPressed && "bg-neutral-900 text-white")} {...props} />;\n  }\n);`;
